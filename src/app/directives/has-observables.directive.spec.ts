import { HasObservablesDirective } from './has-observables.directive';

describe('HasObservablesDirective', () => {
  it('should create an instance', () => {
    const directive = new HasObservablesDirective();
    expect(directive).toBeTruthy();
  });
});
